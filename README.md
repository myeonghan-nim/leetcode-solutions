# LeetCode Solutions

이 저장소는 다양한 LeetCode 문제에 대한 풀이를 담고 있습니다.

## 구조

솔루션은 난이도별(예: `easy/`, `medium/`, `hard/`)로 정리되어 있습니다.
각 문제는 `two_sum.py`와 같이 문제와 동일한 이름의 Python 파일을 가집니다.

## 사용 언어

Python으로 솔루션이 구현되어 있습니다.
